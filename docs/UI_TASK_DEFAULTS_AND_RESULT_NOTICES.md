# 任务默认值与结果提醒（2026-09-22）

- 新任务默认可视化提示词、尾帧承接；保留用户显式保存的其他选择。
- 首个任务无上游，按独立任务执行。其他任务缺少上一任务视频时，提前显示橙色“!”风险贴纸；点击可查看风险详情。
- 单任务和批量生成确认窗口都在提交前列出风险，允许用户继续。该情形的执行快照明确保存风险和独立生成模式，任务原有承接意图不被覆盖。
- 无媒体缺失时继续使用已有上下文完整性校验；跨项目引用、失效来源等错误不因这一例外被忽略。
- 编辑窗底部显示修改状态：任务已保存／任务已修改 · 未保存／新任务 · 未保存；展示模式切换不是任务内容修改。
- 任务和文件夹使用相同的新结果贴纸：青绿色视频、紫色增强提示词，均仅显示“新”。同时存在时视频在上，略错开叠放；文字无黑底和装饰色条。查看对应结果后按本机记录消除提醒。
- 文件夹凸起显示项目真实创建日期，10px、80% 不透明度，移除影视图标。
- 单选按钮位于预览图下方，同一行等宽；“编辑任务”使用笔图标。多选操作位于底栏中央。
