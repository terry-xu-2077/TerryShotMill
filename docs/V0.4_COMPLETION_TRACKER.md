# V0.4 完善与交互验收清单

2026-09-21。范围：当前版本核对发现的问题，以及用户本轮追加的全部交互要求。
本清单记录实施与证据；未验证的项目不视为完成。此前启动器相关未提交修改保留。

## 2026-09-21 · 文件夹动效实机修正

- 直接检查用户正在体验的内嵌浏览器：prefers-reduced-motion 为 true，实际 transition-duration 仅 0.00001s，解释了此前独立浏览器有动画、用户页面却瞬间弹出的差异。清除 app.css 重复的全局减少动效规则，将策略收敛到 tokens.css。
- 用户明确要求保留文件夹弹出动画，因此该局部在减少动态效果模式下仍保留 320ms 短缓动；普通模式为 480ms。不改变系统/浏览器偏好，其他控件继续遵循减少动态效果。
- 内卡默认顶部从容器 28% 调至 40%，高度从 70% 调至 58%；位移从容器约 28% 减至 13.92%，弹出终点约为 26.08%，覆盖之前“顶到容器顶部”的要求。默认更收拢、弹出更克制。
- 用户当前页面已实测生效为 0.32s。六项浏览器回归通过，包括减少动态效果下真实 CSS Transition 存在、时长、位移、回落及相邻布局不变；前端分区检查通过。

## 用户要求

- [x] ComfyUI Bridge 通信状态：连接中、已连接、断开/异常，真实检查与恢复。
- [x] 每任务耗时：增强、视频执行与排队时间来自持久化时间戳，不冒充视频时长。
- [x] 工作流配置：选择工作流 → 名称、简介 → 保存档位预设；编辑器使用预设。
- [x] 资产图片点击放大，点击大图关闭；移除图片独立放大按钮。
- [x] 项目文件夹展示简介、生产进度等有效信息。
- [x] 项目封面铺满预览区；最新体验反馈覆盖原橙暗渐变，改为蓝色半透明文件夹，无封面为灰白。
- [x] 文件夹本体保持横向矩形；最新反馈要求外层正方形预留内卡上滑空间，含高光描边、较窄后层与圆弧转折。
- [x] 单项/批量操作明确“增强提示词”和“生成视频”。
- [x] 排队和正在生成的任务沿用原窗口只读查看提示词与生成参数；允许切换显示模式和复制，隐藏保存/增强/审核操作，完成后自动恢复编辑。
- [x] 已生成任务使用视频截图作缩略图，保留未生成任务资产回退。
- [x] 项目自动封面随第一个任务封面更新；自定义封面不会被自动覆盖。
- [x] 项目配置可选择资产或从视频截帧作为封面，可恢复自动封面。
- [x] 列表、卡片、详情点击结果缩略图直接打开播放器并自动播放。
- [x] 常用操作减少点击成本，保持键盘入口、选择行为及审核门槛；切换任务保护未保存草稿，保存/审核失败允许重试。
- [x] 上下文区间滑块错位与拖动不流畅：统一刻度、即时反馈、端点独立、保存重开与小数精度。
- [x] 上下文区间使用悬浮时间轴：参数区只显示摘要，片段块整体平移保留时长，拖动左右边缘修剪；打开不改变参数区高度。
- [x] 亮色可视化提示词提高文字与标签对比度，对白增加独立底色与醒目边线；暗色同步验证。
- [x] 可视化 / 文本按任务独立记录，用户提示词与 AI 提示词分别保存；切换即记忆，取消和刷新不丢失，保持原有提示词来源语义。
- [x] 上下文实际执行链路验收：片段/尾帧提取、发送生成服务、来源结果与区间快照、上游结果变化及不承接隔离。
- [ ] H3 参考片段内部裁帧与更严格的首帧连续性：真实输入 24 帧，但当前节点裁至前 22 帧；参考承接不保证无缝续帧。
- [x] 新建任务草稿的时间轴同步上一成片实际时长；首次打开、保存和重开均已验证。
- [ ] 自动识别上一条生成视频的切镜点，支持直接选择其中某个镜头片段作为上下文，减少手动拖动区间的操作。

### 提示词配色与显示偏好验收（2026-09-21）

- 暗色专项复查：覆盖用户/AI 两套可视化，1366×768 与 960×540 的对白编辑、选中文字、语言选项焦点与切回文本；正文和全部标签保持文字对比度 ≥4.5、对白边线 ≥3。修复内部焦点切换引起重绘丢焦点，以及移出编辑区后语言修改回调引用失效的问题，补齐键盘焦点边线。两项问题均先由回归复现后修复。
- 暗色增量验收：前端分区三阶段、H3 组件 3 项、相关浏览器 12 项通过；截图已目视检查，证据 `.artifacts/prompt-display/dark-audit/`。首次浏览器运行用例通过但临时数据库清理遇到文件占用，复验正常退出，无遗留测试后端。
- 先复现亮色摘要标签对比度只有 1.43；改为跟随 Accent 的亮暗语义色，全部可视化标签文字及对白文字对比度不低于 4.5，对白边线与编辑区对比度不低于 3。实际截图已目视检查，留在 `.artifacts/prompt-display/light.png` 与 `dark.png`。
- 显示偏好通过独立 PATCH 接口更新现有任务偏好字段，不保存编辑草稿，不修改 Prompt Source、任务 revision、审核、上下文或生成状态。新建未保存任务仍只保留草稿，保存时创建任务并写入偏好。
- 同一任务的快速切换顺序保存，重开等待当前写入；失败显示可重试提示。真实浏览器覆盖两个任务、用户/AI 各自偏好、取消草稿、刷新恢复及失败重试。后端回归覆盖来源/审核/内容不变、跨项目隔离和输入校验。
- 后端分区两阶段、前端分区三阶段、完整接口契约 101 项、完整浏览器 44 项通过。空闲重启本地后端后确认新接口可用，6 个项目保留，对真实任务的空补丁未改变编辑器数据；证据 `.artifacts/prompt-display/live-check.json`。

### 用户补充：按视频内镜头选择上下文（已记录，未实现）

用户希望识别上一条视频内部的切镜，进而直接选择其中某个镜头作为上下文。这里的“镜头”是成片内的连续片段，不是新建 Generation Task，也不改变原有任务顺序。

建议交互：

- 在片段承接区域展示检测到的镜头缩略图、起止时间和时长；点击即可选中并填入上下文区间，同时预览该片段。
- 优先呈现最后一个镜头，允许直接选择更早的镜头；保留“尾帧”和“末尾 1 秒”快捷选择，不将自动检测替代用户当前已保存的区间。
- 选中后仍可微调边界；提供修正误切和补充漏切的入口。没有检测到切镜时，显示整条视频为一个候选片段。
- 检测结果绑定具体来源 Result；更换上一任务的主结果后重新检测并提示原选择过期。排队任务继续使用已冻结的来源与区间。
- 自动检测结果作为可修正的候选边界，不承诺全部正确。验收需包含硬切、渐变转场、闪光、快速运镜及单一长镜头，分别检查误切、漏切与选段准确性。

本次仅记录新增需求和建议交互，尚未修改检测逻辑或界面。

## 可靠性与版本收尾

- [x] 增强队列冻结实际模型、配置、系统提示词，排队后设置变化不改变已提交工作。
- [x] 批次取消未开始项、仅重试失败项、历史记录保留。
- [x] 运行中心取消待执行视频，仅处理当前显示的排队项；不打断运行中任务，取消记录刷新/重启保留，任务恢复可编辑。
- [x] 持久失败原因、运行中心、批次完成/失败统计和恢复入口。
- [x] 生成资格检查与执行校验一致，包括工作流、媒体、数值参数。
- [x] 通用工作流数值端口绑定，秒数/帧数换算，有效参数明确显示。
- [x] 单项增强上一任务摘要独立开关，默认不发送。
- [x] 项目配置等待保存完成，失败保留草稿并可重试。
- [x] 可见多选入口，实际 AI 配置名称替代硬编码名称。
- [x] 批量增强预览实际可增强数量和空提示词/增强中跳过原因，提交前重查状态，检查失败可重试。
- [x] 弹窗焦点进入、限制与返回，嵌套浮层、键盘、缩放布局及减少动效验收；设备与辅助技术边界见下方记录。
- [x] 亮暗主题对比度与可访问性检查；已记录自动测量、键盘/布局与人工截图范围，未宣称完整 WCAG 或读屏认证。
- [ ] 批量部分失败、取消、重试、重复提交、重启恢复、快照不可变回归。
- [x] Prompt Source / Revision / 内容审核失效矩阵。
- [x] 共享组件改动进入组件库源码与 Showcase，固定依赖升级；采用可重建本地版本包，尚未推送上游。
- [ ] 同步版本完成清单和开发/验收记录。
- [ ] 对应分区验证、全量验证、真实 Provider 冒烟与视觉交互复核。

## 实施证据

### 2026-09-21 · 用户体验反馈：菜单、滑块与拟物文件夹

- 回归先复现：档位菜单在参数栏内被压至 45px，超长项目简介把 384px 卡片内容撑至 5102px。共享 Select 改为挂在当前 Modal 的 Portal，按窗口空间选择上下展开，长菜单项换行；触发器保留省略，不侵占总秒数区域。秒数接入共享 Slider，保留键盘与数值输入、保存和生成只读行为。
- 文件夹按新参考改为干净的蓝色磨砂材质，无封面采用灰白纸页；标题、简介、统计有独立文字层级，超长标题/简介末端省略。前面板使用半透明背景与圆弧转折，项目及任务状态使用轻量纸贴质感。
- 后续补充：外层为正方形，为动画留空；文件夹本体仍为扁宽，背景层略窄。内卡增加高光描边，悬停或键盘聚焦时平移上滑，顶部到容器边界，移开柔和回落，邻居和内容布局不移动。新建入口仍为紧凑扁宽形态。
- 文件夹采用 480ms 缓动平移；菜单、弹窗、参数展开、视图切换复用柔和减速节奏，系统减少动态效果仍有效。没有通过缩小字号或新增横向滚动解决布局。
- 最终前端类型/单元/构建三阶段通过，17 项相关浏览器回归通过，覆盖 1366/960/390 宽度、两种主题、长名称、真实鼠标悬停/回落、减少动态效果、保存、播放与只读状态。共享 Showcase 四组主题/尺寸下的窄栏、搜索、禁用及滑块键盘操作通过。
- 半透明面板对比度按黑/白背景极值合成测量，避免假设封面总是浅色；沿用文字 4.5 阈值。真实项目首页、播放器和编辑器复核正常，6 个已有项目保留、Bridge 已连接，未提交生成或修改生产任务。
- 共享库固定为 0.2.1-shotmill.8：补丁重建核对 21 个源码文件，84 个安装文件与归档逐字节一致；未推送上游。证据 `.artifacts/experience-layout/`。本轮不继续切镜检测及原整体计划的其他待办。


### 2026-09-21 · 主题对比度收尾与用户体验交接

- 修复主要按钮、选中标签、亮色普通按钮、引用菜单辅助文字与图标，以及橙色文件夹背景上的状态/简介/统计文字对比度。共享控件修改在 UI Library 源码完成，业务文件夹样式留在应用所属文件。
- 两种主题的应用 42 个测量样本及共享 Showcase 24 个样本通过；普通文字阈值 4.5，引用图标阈值 3。测量解析实际 CSS 颜色、透明背景和线性渐变端点，不等同于逐像素、屏幕阅读器或全部 WCAG 验收。亮暗文件夹与菜单截图已检查。
- 共享依赖固定为 0.2.1-shotmill.7，源码补丁可从指定基线重建；17 个源码文件及 84 个安装文件比对通过，尚未推送上游。
- 本轮全量八阶段通过，浏览器 55/55。最后补充文件夹颜色修复后，前端类型检查、单元测试、构建三阶段，以及主题/项目封面两项浏览器回归再次通过；没有将此前全量运行冒充最后 CSS 修改后的全量重跑。
- 交接前发现前端 1420 未运行，已启动连接真实后端的前端服务。真实浏览器确认首页 6 个项目、Bridge 已连接、上下文测试项目视频自动播放且无解码错误、任务编辑窗正常，无页面脚本错误。服务保留运行，未提交生成或修改生产任务。
- 用户要求先体验再决定后续。当前正在修的颜色问题已收尾；暂停新增开发。待办保留：切镜检测、H3 内部裁帧/严格连续性、队列验收矩阵最终归档及版本文档总收尾。既有回归和实测记录保留，不宣称整体路线图全部完成。
- 证据：`.artifacts/theme-contrast/`，包括 app-measurements.json、showcase/measurements.json、dependency-proof.json、live-check.json 与实际界面截图。


### 2026-09-21 焦点、引用菜单与高密度布局收尾

- 新回归复现可视化引用菜单在 Esc 松键时重新打开，以及文本模式在同次键盘操作触发选区事件时重新打开。修复后，方向键循环选择、Enter/Tab 插入、Esc 只关闭菜单及后续再次输入均正常，焦点留在提示词中，下一次 Esc 才关闭编辑窗并返回任务入口。
- 高密度长内容测试复现超长任务标题使关闭按钮被挤出窗口。业务 Dialog 标题容器允许弹性收缩，任务标题省略显示，导航和关闭按钮保留；同时覆盖只读标题样式。没有修改共享组件内部几何，也没有增加滚动区域。
- 前端分区三阶段、相关桌面 14 项及桌面/移动补充 3 项通过。两种主题下，60 段长提示词只在编辑区滚动，切换文本/可视化和展开参数不改变窗口外框，设置各分类的保存入口可见。截图已目视检查，证据 `.artifacts/final-ui-audit/`。
- 验收范围：1366×768、960×540 CSS 布局视口、设备像素比 1/2，以及既有 390px 移动监看。960×540 对应 1920×1080 在 200% 缩放时的布局空间；像素比 2 由浏览器仿真验证，不宣称实际操作了系统缩放或浏览器缩放菜单。减少动效检查覆盖入场延迟、过渡和主题/选择不重复入场；未据此声称完成屏幕阅读器或全部 WCAG 条款。
- 共享库归档 SHA256 仍为 `2130a074f6bed0c48b8bf2b965f30ba4894595b918848cef852eab765421aa55`，84 个安装文件逐字节一致，16 个源码文件与已重建源码一致。现有 Modal/Select/RangeSlider Showcase 及固定版本 `0.2.1-shotmill.5` 的验收记录有效；未推送共享库上游。
- 曾有一次失败测试结束后的临时数据库清理遇到 Windows 文件占用；随后检查没有遗留该测试后端，最终两组浏览器运行均正常退出。整体颜色对比度仍未勾选，尤其需要继续核对菜单辅助文字、亮色图标、选中标签和主要按钮的实际背景；引用菜单截图中的缩略图来自不可解码的测试占位图片，不作为媒体显示验收证据。

### 2026-09-21 连续查看、草稿保护与审核重试

- 上一个/下一个任务沿用同一编辑窗口；有内容修改时提供“保存并切换 / 放弃并切换 / 继续编辑”，无修改、只切换可视化/文本或只读查看时直接切换。保存失败保留草稿和确认窗，允许重试。
- Alt+左右方向键切换任务，Ctrl+Enter 保存；快捷键遵循生成期间只读和忙碌状态，保存不等于审核，不跳过批量生成的审核门槛。
- 修复内容保存成功但审核请求失败后再次确认产生旧版本冲突的问题：重新读取已保存内容，确认内容一致才更新本地版本并允许重试；检测到不同内容或读取失败时保留草稿并提示重新检查，不直接覆盖新内容。
- 前端分区三阶段通过；任务切换、浮层、上下文时间轴和生成只读共 11 项浏览器回归通过。确认窗截图已目视检查，证据 `.artifacts/task-navigation/`。这轮未调用真实模型，也未修改生产任务；整体缩放/可访问性等独立验收项继续保留。

### 2026-09-21 视频待执行项取消

- 运行中心视频区增加“取消待执行视频”，复用已有操作等待和错误重试反馈。请求携带当前排队 Job 的稳定 ID，后来新提交的任务不会被扩大取消；已开始或结束的任务自动跳过。
- 后端在同一事务中检查全部 ID 的项目归属后取消 queued 项，不调用 Provider 中断。原提示词、参数快照和所有结果保留；有成片的任务恢复已完成状态，其余恢复可编辑内容状态，同时保留仍活动的增强/视频状态。
- 集成回归覆盖跨项目/缺失 ID 全部拒绝、重复取消、历史/主结果保留、取消后重新编辑、正在运行任务继续完成，以及取消项重启后和重复投递时不执行。
- 浏览器通过真实 API 取消隔离测试库中的视频排队项，确认运行中的模拟视频完成，取消项无输出，刷新后仍显示已取消。测试使用仅存在于 E2E 服务的可释放等待点，不依赖固定长延迟，也不调用真实模型。
- 全量八阶段通过，桌面/移动浏览器 50/50、完整契约 109/109。空闲更新本地后端后，6 个项目、15 个任务及运行历史指纹一致；新接口使用空 ID 列表验证，不取消真实任务。证据 `.artifacts/video-queue-cancel/acceptance.json`。整体交互和可访问性等未勾选项继续保留。

### 2026-09-21 生成期间只读查看

- 列表/卡片右键入口在排队或生成中显示“查看任务”，双击仍打开同一个窗口。标题、工作流、秒数、上下文及输入资产不可改，提示词正文和对白控件只读；保留用户/AI 内容查看、可视化/文本切换及复制。
- 只读窗口只保留关闭动作，参数可照常展开，不新增页面、权限设置或复杂操作。实时工作台状态驱动只读开关，结束生成后同窗恢复编辑；只读期间浏览的提示词来源不会变成新的生成来源，显示偏好保持。
- 浏览器覆盖排队→生成→完成、键盘输入被阻止、无内容写请求以及完成后恢复编辑；组件回归覆盖正文、对白语言、参数与动作限制。后端两项回归确认现有 TASK_BUSY 拦截仍有效，同时允许读取和保存纯显示偏好。
- 最终前端分区三阶段通过；队列浏览器 2/2、提示词偏好/亮暗浏览器 4/4、后端只读回归 2/2 通过。复核运行中只读窗口截图，参数与关闭入口完整可见。此次无需修改后端业务或新增数据字段。

### 2026-09-21 视频队列恢复与双队列隔离

- 新增回归复现视频队列启动不恢复待执行任务、同一 Job 重复投递重复生成，以及增强开始/结束覆盖正在生成的视频状态。视频队列现在从持久化记录恢复 queued/running，终态 Job 和同进程并发重复投递不再执行；两类队列保留各自执行与历史。
- 恢复中的 running Job 通过独立 Provider 能力读取原服务任务。ComfyUI Adapter 只 GET 原逻辑 Job 状态与输出，不上传、不重新提交、不卸载模型；服务不支持恢复或原任务丢失时明确失败，保持原始开始时间和输入快照。单机单进程是本版执行边界，不宣称分布式多进程幂等。
- 集成回归覆盖 20 条视频排队恢复、顺序、单项失败后继续、失败重试保留历史、重复投递、原任务恢复/不可恢复，以及同一 Task 的增强与视频同时运行时成功/失败隔离。契约补齐状态不存在、超时、失败、空输出与畸形响应。
- 完整契约与两组队列回归 122 项通过。真实 Bridge 读取异星边境既有成片，下载哈希与本地文件完全一致，原任务状态及队列不变；证据 `.artifacts/queue-recovery/live-resume.json`，没有重新运行模型。
- 全量验收八阶段通过，桌面/移动浏览器 48/48。确认全部队列空闲后更新本地后端，6 个项目、15 个任务的编辑器数据和运行历史与更新前完全一致，证据 `.artifacts/queue-recovery/acceptance.json`。视频排队取消的产品入口尚未完成，不能据此勾选整个队列矩阵；其他未勾选范围继续保留。

### 2026-09-21 批量增强预览增量

- `prompt_batch_eligibility` 与 `create_prompt_batch` 共用提示词及活动任务判断；明确检查范围为用户输入非空和无活动增强，媒体解码及 Provider 推理错误仍由执行阶段报告。
- 确认窗展示选中数、可增强数与跳过原因，仅提交合法集合；提交前再次核对，集合变化停留在确认窗。检查失败禁用提交，支持重试和主动重新检查，保留上下文开关。
- `test_prompt_batch_eligibility.py` 验证预览不改项目/任务/活动 Job，不调用模型、不增加队列；覆盖请求去重、任务排序、范围隔离和 queued/running 跳过，并与实际提交结果比对。现有取消/重试/恢复回归保持通过。
- `prompt-batch-eligibility.spec.ts` 验证只提交合法任务、空任务不产生增强历史、状态变化重新确认、失败重试与键盘切换上下文。亮暗 960×540 截图已目视检查。
- 全量验证 7 阶段通过，桌面/移动 48/48，完整契约 101/101。本地 6 项目、15 任务以及运行历史重启前后指纹相同，预览接口不产生新运行记录；证据 `.artifacts/prompt-batch-preview/`。
- 入口追踪确认旧 Toast/FullscreenPreview 仍属演示/旧编辑器代码，当前 App 生产路径使用任务播放器和正式 Dialog；本增量没有为演示入口扩充功能。双队列并行/恢复的完整证据和剩余可访问性边界仍继续核对。

### 2026-09-21 实现与验收记录

本轮仍在进行中。勾选项有下列代码和测试证据；未勾选项不能因整体检查通过而视为完成。

- `test_prompt_batch_recovery.py` 验证取消只影响 queued、running 允许完成、重复取消、重复重试防重、失败记录不被覆盖、跨项目拒绝、取消后重启，以及 20 个任务按故事顺序恢复。恢复前修改设置和原本为空的项目背景，执行仍使用提交时快照。
- `test_prompt_profile_snapshot.py` 通过真实 Adapter + MockTransport 检查冻结后的 ComfyUI endpoint、模型、帧数、尺寸、随机种子、系统提示词，以及 API endpoint/model。数据库快照不复制 API Key；授权凭据变化时明确失败，要求重新提交，不静默替换凭据或任务配置。旧缺少完整配置的可配置 Provider 队列任务同样要求重新提交。
- `RuntimeCenter` 展示持久化增强批次和视频任务历史、时间、失败原因、取消待执行项、重试失败项。`runtime-center.spec.ts` 通过真实 API 队列注入首次失败，刷新页面后重试，仅失败项进入新批次，原失败历史仍可见。桌面渲染截图已检查。
- 假 API 已移除临时内存批次和伪提交路由，复用正式 Service/Repository/Router。视频测试 Provider 返回仓库内可解码的 2 秒 MP4，不连接真实生成服务。`test_mock_api.py` 检查正式契约一致、批次入库、视频结果可读取。
- `test_bridge_status_api.py` 覆盖正常、未安装、HTTP 故障、超时、非法健康响应和 ComfyUI 断开；状态检查不读取工作流目录。前端测试覆盖手动恢复和后端不可用状态。只读检查本机 Bridge 的 `/shotmill/v1/health` 返回 `ok=true`、`package=ComfyUI-ShotMill`。
- `test_application_settings_api.py` 验证档位名称、简介、文件及默认选择写入并重新加载；设置组件测试验证选工作流后直接保存档位，任务工作流选择有已有浏览器回归。
- `project-cover.spec.ts` 使用真实生成 API 和本地测试视频，校验 PNG 截帧、首任务封面、横向卡片比例、背景 cover 铺满、简介/进度，保存图片封面、视频截帧封面与恢复自动封面；切换配置页签保留未保存截帧；资产图点击放大/关闭。列表、卡片、详情三个入口均校验视频实际处于播放状态且播放时间前进。
- `test_project_cover.py` 验证跨项目封面拒绝、截帧失败不更新项目标题、使用中的封面资产不可被直接删除。配置内主动移除封面资产时先恢复自动封面。
- `ProjectConfigPanel` 测试验证异步保存完成前不关闭，失败保留修改并可重试；`TaskEditorDialog` 测试验证上一任务摘要默认不发送，手动启用后发送。
- 亮、暗首页文件夹截图已检查。仍需完成整个界面的对比度、焦点、缩放和长内容验收。
- 2026-09-21：全部 73 项 Contract Tests 通过；`scripts/verify.py --full` 的 7 个阶段通过（后端 lint/tests、前端 typecheck/Vitest/build、Provider contracts、桌面/移动 E2E）；浏览器测试 30/30 通过。Windows 命令进程使用 PowerShell 7，pnpm 子脚本设置 `npm_config_script_shell=C:\Program Files\PowerShell\7\pwsh.exe`。该结果证明本轮实现的回归通过，不代表上文未勾选功能已完成。
- E2E 运行器现支持按文件筛选；Windows 停止其拥有的 Python 启动器及子进程，避免结束后遗留占用临时数据库的 uvicorn。针对原清理失败的回归及浏览器重跑均通过。

### 当前明确未收尾项

- 任务耗时、可见多选、全项目增强入口与实际模型名称已补齐，证据见下节；完整交互收尾仍受下列未完成项约束。
- 工作流数值绑定、完整内容冻结和原生节点预检已完成本机验收；媒体实际解码、显存及模型推理错误仍由运行阶段报告，不承诺预检能消除所有运行失败。
- 批量增强提交前的可执行/跳过数量预览已补齐：后端只读资格接口与提交共用空提示词/活动增强判断，前端只提交已确认的合法任务，数量或任务集合变化先重新确认。
- 共享 UI 组件焦点、键盘、布局及固定依赖已验收，采用附源码补丁的本地版本包，尚未推送上游。整体对比度、队列全矩阵最终核对、最新全量验证和最终开发文档收尾仍待完成。

### 2026-09-21 续增量：耗时、选择与审核

- 最近增强尝试包含成功、失败、取消和待执行状态；增强/视频排队与执行时间分别计算。`measuredAt` 与运行标记支持前端继续计时，完成后停止，没有历史时间戳时保持未知。独立任务摘要与工作台使用同一计算器。
- 已保存任务的单项增强保留正式 Prompt Job / Batch 尝试记录，失败也有时间与错误；仍同步返回原增强接口结果。重复增强活动任务返回 409；失败重试使用原编辑器输入快照，保留用户/AI 来源语义。新任务草稿预览仍不创建 Task 或 Job。
- `test_task_timing.py`、`taskTiming.test.ts` 和 `test_prompt_batch_recovery.py` 覆盖上述数据与时间行为。`runtime-center.spec.ts` 验证刷新后成功/失败任务耗时在列表、卡片及详情中可见。
- 两种任务视图均提供共享 Checkbox，全选/取消全选可见，保留 Ctrl/Shift 和 Space 键选择。新增工作台“批量增强提示词”：无勾选处理全项目，有勾选只处理选中任务，确认窗冻结本次请求范围。
- 模型名称由实际 Provider Adapter 返回，覆盖本地模型、API 设置和环境回退；不复制前端硬编码模型名。契约与组件回归已通过，假数据模式明确显示演示服务。
- `test_batch_review_api.py` 共 18 项通过，覆盖用户/AI 两种来源下有效内容、未使用内容、同文本来源切换及切回、显示模式、标题、清空内容、再次增强、相同文本不同 AI 版本切换，并验证生成资格与审核状态一致。
- 浏览器检查发现并修复窄窗口顶栏项目名与 Bridge 状态重叠；两行自适应布局保留操作与状态。`workspace-controls.spec.ts` 检查可见勾选、Space、全选、跨视图选择、默认全项目增强、选择范围以及 390px 顶栏无重叠。桌面与窄窗口截图已检查。
- 本增量最终运行 `scripts/verify.py --full` 七阶段全部通过，桌面/移动 E2E 32/32；另运行全部接口契约 74/74。新任务增强 E2E 改为精确定位编辑窗内按钮，避免新增批量增强入口后模糊名称匹配两个按钮；验证范围与断言保留。
- 只读复核本机 Bridge 健康与工作流目录均为 HTTP 200。`异星边境_稳定720P.json` 暴露 9 个 IMAGE 和 1 个 STRING 输入，没有 INT/FLOAT 端口；不能声称该工作流已接受任务时长控制。本轮未提交真实 GPU 生成。Bridge 完整源码在 `integrations/comfyui_shotmill/`，后续数值端口工作应同时检查 Bridge 与 Provider 合同。

### 2026-09-21 续增量：生成预检与提交一致性

- 新增 Provider 只读验证边界。生成资格检查和正式提交使用同一个 `GenerationService.validate_task`，与执行共用 `VideoGenerationRequest` 构造逻辑；检查素材归属、文件存在、服务时长/媒体能力，再使用 ComfyUI Adapter 的工作流注册、可执行状态、端口类型与槽位匹配检查。预检不上传素材、不创建 Job、不启动生成。
- Provider 检查结束后，正式提交重新读取任务并检查版本、active Video Job 和必要的 Prompt 审核；检查过程中修改任务或撤销审核不能继续提交。Prompt 增强改变任务显示状态也不能绕过已有视频 Job 的防重复检查。
- 资格响应带可选错误代码和原因，确认窗显示具体原因；无细节时不增加 null 字段，保留已有响应形状。单项错误仍不阻断其他合法任务。
- ComfyUI 地址与已解析的工作流相对文件名进入不可变 Job Provider 配置；执行从已保存配置重建 Adapter，不再读取排队后修改的设置。缺少配置的旧 ComfyUI Job 明确要求重新提交。
- `test_video_preflight.py` 九项覆盖工作流缺失/不可执行、媒体类型不匹配、文件缺失、服务时长限制、检查中修改任务、撤销审核、活动视频防重，以及更改设置并重建 Adapter 后仍使用原端点和分组工作流名。界面组件回归检查具体拒绝原因与禁用空提交。
- 尚未冻结远端工作流文件内容；目前只冻结地址、文件选择与任务输入。Bridge 数值输入、工作流内容版本及编译级预检仍需继续实现和真实验收，不能据本节勾选“完整生成资格”完成。
- 本节改动完成后重新运行 `scripts/verify.py --full`，七阶段全部通过，桌面/移动 E2E 32/32；另外全部契约 74/74 通过。真实 Provider 生成与新增数值绑定仍未验收。

### 2026-09-21 续增量：数值档位与 Bridge 实跑

- 档位支持 INT/FLOAT 输入使用工作流默认值、固定数值、任务秒数或按秒数换算帧数；帧率、倍数、偏移可配置。执行向上取满足规则的帧数，例如 6 秒、24 fps、4n+1 得到 145 帧。任务编辑器显示实际换算值；无时长绑定时明确说明使用工作流默认值。
- 数值绑定随档位保存并进入 Video Job 的配置快照；排队后修改设置不会更改已提交的绑定。Provider 提交前拒绝失效端口、重复绑定、非有限数值及非整数 INT。数值端口不占媒体槽位，Bridge 仅修改执行副本，未绑定时保留工作流默认值。
- 单元测试验证换算与拒绝条件；设置 API 验证重新加载，Provider 集成验证快照不变，Bridge 合同验证默认值、类型、稳定端口与原文件不变。浏览器验证 6 秒显示 145 帧、改为 7 秒显示 169 帧，保存重开仍一致；截图位于 `frontend/test-results/workflow-duration.png`。
- 本增量全量验证七阶段通过，E2E 32/32。随后新增 API 格式工作流回归，复现并修复数值名称误判为媒体及同源多输入连接遗漏；Bridge 合同 29/29、Provider 分区验收通过。
- 本机 Bridge 已从 0.1.0 升级到 0.2.0。升级前确认队列空闲，完整插件和索引备份在 `.artifacts/bridge-numeric-20260921/`；按原启动参数重启。17 项已有记录逐项比较一致，其中 13 项完成、4 项原本即为 not_found；未伪造缺失状态。
- 正式 ComfyUI Provider 经配置快照、校验、Bridge 注入与原生 CreateVideo/SaveVideo 实跑：2 秒与 3 秒在 24 fps、4n+1 下分别输出 49 帧 / 2.041667 秒、73 帧 / 3.041667 秒。两个 MP4 均完整解码，证据为 `.artifacts/bridge-numeric-20260921/numeric-evidence.json`。独立临时工作流已按哈希核对后移除，测试结果与历史保留。
- 上述实跑证明数值绑定及视频编码链路，不代表真实模型生成验收。用户现有 `异星边境_稳定720P.json` 未暴露数值端口，仍使用其保存时长；未修改该工作流或默认档位。真实模型增量冒烟、工作流内容冻结及编译级预检仍待完成。
- 最后复核全部契约 81/81、已安装 Bridge 节点合同 14/14；8 个已有结果通过升级后下载并与原文件逐字节哈希比对一致，记录为 `preserved-downloads.json`。结束时 ComfyUI 队列为空，工作流目录只保留用户原工作流。

### 2026-09-21 用户补充：上下文滑块与链路核查

- 浏览器复现原控件错位：两个原生 range 的 min/max 不同，选区按完整时间轴绘制，拖选区右端时未命中实际手柄，1–5 秒无法拖至 1–3 秒。旧单元测试仅直接触发 change，没有覆盖真实鼠标命中。
- 在共享 UI 库基线源码增加 `RangeSlider` 与可交互 Showcase，打包固定版本 `0.2.1-shotmill.2` 并升级依赖；完整源码差异保留在 `frontend/vendor/terry-ui-capsule.patch`。移除 ShotMill 私有双 range DOM、轨道/手柄 CSS，业务只负责区间值。
- 双端使用同一刻度和几何，拖动在控件内实时更新并限制外部通知频率，释放时立即提交；Pointer Capture 保留移出轨道后的拖动，重叠命中区域按最近端点选择。提供方向键、Home/End、PageUp/PageDown 和可访问数值。此前取整丢失小数区间的问题同样修复。
- 新增浏览器回归实际拖动、选区与手柄中心对齐、端点靠近后的独立拖动、键盘修改、保存重开；编辑器 20 项单元测试通过，含上一任务不足 1 秒时保留 0.15–0.65 秒。Showcase 亮暗两种主题、禁用态与 200% 缩放下实际拖动均通过，截图已检查。
- 链路核查发现 `GenerationService` 保存了 `context_snapshot.links`，但 `_provider_request` 尚未消费这些关系并构造片段/尾帧媒体。滑块和持久化通过不能证明实际生成已承接；该链路列为下一优先修复项，不能勾选上下文整体完成。
- 滑块增量全量验证七阶段通过、浏览器 33/33；共享库源码构建与实际安装产物一致。此前一次单项测试功能通过但临时数据库清理碰到 Windows 文件占用，重跑单项与全量后清理正常，未通过忽略错误隐藏问题。

### 2026-09-21 上下文媒体链路与第一场实测

- 补齐真实尾帧/片段提取、来源结果/区间/哈希快照，以及缺失、过期、跨项目和不承接隔离检查。提取在本地完成，Provider 预检不上传素材、不启动 GPU。
- 上下文区间使用实际成片时长；新增浏览器回归覆盖计划 6 秒但实际 2 秒时的 1–2 秒默认承接。
- Bridge 0.2.1 增加原生视频解码链的 H3 引用识别与空视频输入断开；独立测试工作流和档位用于同提示词、同种子的两版对照，原工作流与默认档位保留。
- 真实媒体上传哈希与任务快照匹配，尾帧逐像素匹配原视频最后一帧；片段为 7–8 秒、24 帧。当前 H3 内部将 24 帧裁至 22 帧的限制明确记录，未修改模型算法。
- 全量七阶段通过，既有 E2E 33/33；新增实际时长后上下文 E2E 2/2；完整契约 84/84。详细输入、证据及最终结果见 [第一场上下文实测](./CONTEXT_ACCEPTANCE_2026-09-21.md)。
- 两个镜头、三个结果全部生成并完整解码。尾帧版约 5 分 26 秒，末尾 1 秒版约 5 分 45 秒；后者暴露输入视频预览被当成输出的问题，新增两项失败回归后升级 Bridge 0.2.2 修复。从原 Prompt ID 下载既有成片并保存恢复审计，没有重新生成或覆盖历史 Result。
- 输出回收修复后完整契约 86/86，代码检查通过；最终三条成片及静音左右对照均保存。项目整体完善任务仍未全部完成。

### 2026-09-21 新建任务的实际成片时间轴

- 浏览器先复现：上一任务计划 6 秒、实际视频 2 秒时，新建任务显示 5–6 秒，区间超出成片。结果摘要增加独立的实测时长，前端新建与已保存任务均优先使用上一主结果的时长；计划时长仍保留原含义。
- 旧结果已有封面但缺少时长、或原元数据记录了错误时长时，也以文件实测值为准。查询只补充返回的摘要，不改写历史 Result。视频探测按路径、修改时间和文件大小缓存；同路径替换文件后重新测量。
- 上下文浏览器回归 3/3：新任务首次保存为 1–2 秒、已保存任务实际时长、真实拖动与刷新重开。媒体/上下文/封面 targeted 测试 11/11，后端分区与前端分区验证全部通过。
- 确认本机增强/视频队列空闲后重启本任务启动的后端，使新结果摘要在实际应用中生效。工作流完整内容冻结与编译级预检仍待继续完成。

### 2026-09-21 工作流完整内容冻结

- 先增加失败回归，复现 Job 只保存文件名、未保存工作流内容的问题。Bridge 0.3.0 新增只读快照接口，一次读取得到原画布、编译后的执行图和输入端口，附规范化 SHA-256；后端将完整内容存入不可变 Job 配置。排队后不再读取当前工作流目录，重建 Adapter 仍执行原快照。
- 目录查询与快照读取之间文件发生变化时，以快照内端口重新检查资产和数值绑定。损坏快照、旧 Bridge、编译失败和无完整快照的旧排队任务均明确拒绝，不静默读取今天的工作流。Prompt AI 直接提交 prompt 的路径保留。
- 生成预检仍不上传媒体、不排 GPU 队列。画布编译已提前；原生节点缺失、模型选项与运行时输入的完整校验目前仍在执行阶段，尚不能勾选完整生成资格校验。节点实现和模型文件不属于本次工作流内容冻结范围。
- 本机 Bridge 与后端在确认全部队列空闲后更新。升级前保存 24 条 Bridge 状态、6 个项目；重启后逐条比较状态，测试结束比较各项目的视频历史和增强批次，均一致。两个用户工作流哈希不变，其只读快照分别编译为 43 和 40 个节点，未启动模型生成。
- 真实原生节点验收先保存橙色 25 帧版本，再改文件保存蓝色 49 帧版本，删除临时工作流后，重建 Provider 分别执行两个已序列化快照。输出均完整解码，分别为 24 fps / 25 帧 / 1.041667 秒和 24 fps / 49 帧 / 2.041667 秒；解码颜色符合各自快照。证据与备份位于 `.artifacts/workflow-snapshot-live/`，`evidence.json` 记录来源删除、快照哈希、成片哈希和历史保留结果。
- 补齐上一增量 `PrimaryResultView.durationSeconds` 遗漏的 OpenAPI 契约快照；契约测试保持严格比较，没有放宽断言。最终后端分区两阶段、完整契约 92/92、Provider 分区均通过。快照新增集成回归覆盖重建后执行不变、编译失败、服务无快照接口、损坏内容、端口竞态与畸形响应。

### 2026-09-21 原生节点生成前校验

- 新增失败回归，复现缺失模型仍能创建 queued Job；Provider 现在在创建 Job 前调用 Bridge 0.3.1 的只读预检。预检与执行共用冻结工作流、最终提示词、种子、媒体槽位、数值输入、可选引用断开和输出改写；错误保留节点位置及具体原因，常见原因使用中文，不把原始 JSON 堆给用户。
- 使用 ComfyUI 自身的输入与循环检查，覆盖节点安装、必填输入、连接类型、模型选项、数值范围及循环依赖。仅由实际 Bridge 绑定推导出的 LoadImage/LoadVideo/LoadAudio 单文件输入延后远端文件检查；客户端无法指定任意跳过节点，未替换的工作流自有文件继续检查。没有修改全局 validator、节点注册或模型算法。
- 实测发现原生 V3 动态选项展开会丢弃非法值，补充共用的动态选项检查；真实 H3 画布又复现通配类型被误判，增加对应回归并修正。视频工作流的某个有效输出失败时整体拒绝，避免无提示地只生成其他输出；Prompt AI 直接 prompt 路径保持原行为。
- 14 个本机场景通过：原生合法图、缺失节点、缺失必填项、数值越界、类型不匹配、错误动态选项、图片/视频/音频等待上传、带待上传图的缺失模型、未替换文件缺失、循环依赖，以及两份实际 H3 画布。预检前后输入目录文件状态、队列与 Job 索引完全一致，26 条 Bridge 历史和 6 个项目记录保留，证据为 `.artifacts/preflight-live/evidence.json`。
- 额外通过正式 Provider 完成“预检 → 执行 → 回收”，使用已删除源文件的原生视频工作流快照，输出 24 fps / 25 帧 / 1.041667 秒，完整解码通过，证据为 `execution-proof.json`。没有重新运行 H3 模型生成。
- 后端分区、Provider 分区、完整契约 101/101 通过；最终全量验证七阶段全部通过，浏览器 35/35。结束时 Bridge 已连接、队列为空，安装文件与源码哈希一致，最终状态保存在 `final-status.json`。此项完成不代表可访问性、队列全矩阵、共享库收尾或其他未勾选项完成。

### 2026-09-21 共享模态层与键盘交互（收尾进行中）

- 浏览器先复现 Tab 离开弹窗、嵌套大图未获得焦点，以及工作流菜单 Esc 连带关闭任务编辑窗。共享库新增 Modal，使用原生 dialog 隔离背景、约束 Tab/Shift+Tab 并恢复入口焦点；ShotMill 的业务 Dialog 复用它，子菜单 Portal 保留在当前原生模态层内。
- Esc 由内部控件优先处理，未消费的事件再交给统一浮层栈。共享 Select 支持方向键、Home/End、回车选择、Esc 关闭和焦点返回；搜索输入保留正常文本编辑键。没有在 ShotMill CSS 修改共享控件内部结构。
- modal-focus.spec.ts 的 5 个浏览器场景通过：新建项目焦点与背景隔离、嵌套资产图片、工作流菜单键盘、视频自动播放与焦点返回，以及两种主题下 1366×768 / 960×540 布局。后者对应 1920×1080 屏幕 200% 的 CSS 布局视口，不等同于实际操作浏览器缩放 UI。
- 实际运行的共享库 Showcase 已挂载 Modal / Dialog / Select 示例；两种主题、两种尺寸、嵌套关闭及减少动效均通过。截图已检查，证据在 .artifacts/modal-showcase/。亮色示例部分普通按钮文字偏淡，留待对比度测量修正，未宣称完整可访问性验收。
- 固定依赖 0.2.1-shotmill.4；源码补丁可从 ba1474ac56ad9b15a2f09d51a1c910b811b83ec9 重建，16 个源码文件逐项比对（仅规范化换行），已安装的 84 个文件与打包内容逐字节一致。JS/CSS 与构建输出一致，证据见 dependency-proof.json。没有推送共享库上游。
- 前端分区三阶段通过。首次完整浏览器回归 39/40；唯一失败发生在滑块重开后的展开动画中，取坐标后控件继续向下移动，鼠标落在轨道外。已补齐与首次打开一致的动画完成等待，并增加实际命中检查，保留原有区间、端点独立和保存重开断言；连续三轮上下文测试 9/9 通过，之后完整浏览器回归 40/40 通过（时间轴新需求前的模态层版本）。
- 整项仍未勾选：后续核对原生模态层与全局通知、旧全屏预览的叠放关系，补全资产菜单键盘与亮暗对比度、读屏检查边界。切镜检测保持仅记录。


### 2026-09-21 悬浮时间轴片段交互

- 用户明确了最终交互：像非线性编辑软件一样把选区当成片段块，并放入悬浮控件，不能把参数区顶高。参数区现在仅显示起止时间与时长摘要；点击展开锚定时间轴，空间不足时向上打开，宽度随视口约束。
- 共享 RangeSlider 增加 timeline 展示：刻度尺、片段块、修剪边缘。拖片段整体平移并保持时长，拖边缘只修改对应端点；超过视频边界时整体夹到边界。支持方向键、Home/End 微调和小数区间，保留普通 slider 形式。
- 时间轴通过现有 Popover/Portal 挂在当前原生模态层内。打开后聚焦片段，完成或 Esc 收起并回到摘要入口；外部点击收起但保留草稿。最终仍由任务“保存”落库。
- 浏览器 4 个上下文场景通过，包括实际成片时长、整体平移、边界夹取、两端修剪、近距离端点、键盘、保存重开、Esc 只关闭浮层、参数区高度不变。两种主题的 1366×768 与 960×540 截图已检查。早期内嵌方案的小窗口读数裁切已由悬浮方案解决，回归继续检查读数完整可见。
- 增加 0.75 秒来源、0.15–0.65 秒片段的键盘移动回归，确认边界处仍保持 0.5 秒时长。任务编辑器与浮层组件测试共 27 项通过；既有异步槽位测试仍输出 act 提示，未弱化断言。
- 共享库固定版本为 0.2.1-shotmill.5，普通/时间轴两种形式及禁用例均在实际 Showcase 展示，四组亮暗/尺寸验收通过。源码补丁重建、84 个安装文件与归档逐字节对照通过，证据在 .artifacts/timeline-clip/。最终前端分区三阶段通过，桌面/移动浏览器 41/41 通过。悬浮层的“完成”、外部点击及 Esc 均保留草稿，Esc 返回摘要焦点并保持编辑窗开启。
