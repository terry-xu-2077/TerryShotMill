# 提示词质量改进：实施与验收

状态：**仅记录，暂不实施自动循环审查。** 用户最新决定先把 Gemini 导演智能体原则融入 H3 系统提示词并跑样片。刚开始接入的循环代码、数据库字段与迁移已撤回；以下保留为后续方案，不是当前已实现功能。

用户目标：减少低质量结果。模型选择仍属于 Provider，不进入 Core。

## 执行顺序

1. 冻结用户意图、明确启用的上下文、实际媒体、时长与增强配置。
2. 独立导演规划：组织本任务内完整叙事单元，明确空间关系、动作过程、镜头动机、光路与声音。尊重实际时长，生产规划优先约 15 秒，不能擅自把现有任务改成 15 秒。
3. 按用户的系统提示词和目标 Skill 编写最终提示词，保留导演意图。
4. 新请求独立诊断：携带原始输入、同一批实际媒体和候选文本；不携带写作推理过程。六项检查必须全部返回，具体问题附证据与修正建议。
5. 程序检查引用与显式镜头时间；最多修订两次，每次重新诊断。格式无效、服务失败或严重问题未消除均不得视为通过。
6. 通过后仍进入人工待检查；自动检查不能写入人工批准。失败不得覆盖旧 Prompt 或生成伪成功 Revision，其他任务继续。

## 验收范围（尚未全部完成）

- [ ] 独立诊断不会被写作系统提示词覆盖；真实媒体在所有阶段保持一致。
- [ ] 导演规划、写作、诊断、两轮上限与程序检查有自动测试。
- [ ] 报告和阶段可追溯，失败报告也持久化；不泄漏密钥或资产绝对路径。
- [ ] 单任务、草稿与批量复用同一流程；冻结配置、人工审核、任务修改检查继续有效。
- [ ] 批量部分失败、取消、恢复与快照不可变回归通过。
- [ ] 界面能看见阶段和未解决的问题，自动检查与人工审核用语明确区分。
- [ ] 真实 Qwen 诊断与修订验收，包括车尾驾驶等具体错误；不能仅以模拟测试代替。
- [ ] B/C/D 对照样片完整分析并记录限制；代表性约 15 秒样片经实际观看确认，再决定生产基线。

已取消的“异星边境 · 电影第一、二场 · 720P”不得恢复；质量诊断不自动批量生产。
