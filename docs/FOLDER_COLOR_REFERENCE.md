# 文件夹蓝色备选记录

2026-09-22：用户认可蓝色本身，但认为不适合当前深色背景。项目文件夹统一改用此前空项目的中性配色，保留以下蓝色供未来使用。

- 基础色：`--sm-folder: oklch(from var(--tc-accent) .78 .115 230)`，即 L=0.78、C=0.115、h=230°，无额外透明度。
- 前景基础：`color-mix(in srgb, var(--sm-folder) 83%, white)`。
- 前景实际渐变：120°，上述前景基础色从 82% 不透明度渐变到 74%。
- 背景层：基础蓝色 50% 与白色混合。
- 前景玻璃效果：`blur(10px) saturate(.8)`。

这是代码中的准确色彩定义；截图观感还受封面、透明叠加和背景影响，不能用一个截图取样色替代整套参数。原 `--sm-folder` 色彩 token 继续保留，本次仅停止用于项目文件夹。

中性版本的深色文件夹：背景 `#30343a`，主体 `#35383e`，前景 `#3d4148`；标题 `#f1f3f5`，简介 `#d9dde3`，辅助信息 `#cdd3db`。

后续用户调整：所有文件夹轻微跟随当前主题色。采用 `--tc-accent` 的色相，色度降至主题色的 20%；深色主体明度 0.34、前景 0.39，背景明度 0.31、色度 18%；浅色主体明度 0.78、色度 20%。保留原有半透明前景及文本层级。上面的亮蓝色继续作为备选存档。
